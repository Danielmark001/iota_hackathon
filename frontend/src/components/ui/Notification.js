import React from 'react';
import { 
  Snackbar, 
  Alert, 
  Typography 
} from '@mui/material';
import { useSnackbar } from '../../context/SnackbarContext';

const Notification = () => {
  // This component is no longer needed since we're using the SnackbarProvider's built-in component
  // This is just a placeholder to avoid import errors
  return null;
};

export default Notification;
